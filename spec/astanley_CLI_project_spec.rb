RSpec.describe AstanleyCLIProject do
  it "has a version number" do
    expect(AstanleyCLIProject::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
